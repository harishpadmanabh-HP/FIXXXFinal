<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hp.hp.fixxx">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/fixittt"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CategoryChoose"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_category_choose"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".UserLOGIN">

        </activity>
        <activity android:name=".UserSignUp" />
        <activity android:name=".UserHome" />
        <activity android:name=".LabourLogin" />
        <activity android:name=".Category_selection" >

        </activity>
        <activity android:name=".LabourHome" />
        <activity android:name=".joblist"></activity>
    </application>

</manifest>